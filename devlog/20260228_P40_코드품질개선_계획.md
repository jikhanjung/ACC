# P40: v0.0.6 코드 품질 개선 계획

**날짜**: 2026-02-28
**유형**: 계획 (P)
**상태**: 완료

## 목표

v0.0.5 안정 릴리스 이후 코드 품질 부채를 해결하여 유지보수성과 디버깅 용이성 향상.

## 계획 항목

### 1. 매직넘버 상수화
- `acc_core.py`: `THETA_MAX_DEGREES = 180.0`
- `acc_core_new.py`: `THETA_MAX_DEGREES`, `DEFAULT_SIMILARITY = 0.5`

### 2. print → logging 전환
- `acc_gui.py`의 모든 `print()` 호출을 `logging` 모듈로 전환
- 모듈 로거 추가, `main()`에 `basicConfig()` 설정

### 3. bare except 수정
- `acc_gui.py`의 `except:` → `except (ValueError, AttributeError):`
- `pyproject.toml`에서 `E722` 규칙 제거

### 4. plot_acc2() 리팩토링
- 356줄 단일 메서드를 8개 private 메서드로 분리
- `import copy`를 모듈 최상단으로 이동

### 5. GUI 테스트 추가
- `tests/gui/test_acc_gui.py` 신규 생성
- `tests/conftest.py`에 QApplication fixture 추가

### 6. 종합 검증
- ruff check/format, pytest 전체 통과 확인
- CHANGELOG.md, devlog 문서 작성

## 의존성

Tasks 1-3 병행 → Task 4 → Task 5 → Task 6

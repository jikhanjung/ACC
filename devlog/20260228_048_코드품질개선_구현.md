# 048: v0.0.6 코드 품질 개선 구현

**날짜**: 2026-02-28
**유형**: 구현 결과
**관련 계획**: P40

## 변경 요약

### 1. 매직넘버 상수화
- `acc_core.py`: `THETA_MAX_DEGREES = 180.0` 추가, 1곳 교체
- `acc_core_new.py`: `THETA_MAX_DEGREES`, `DEFAULT_SIMILARITY = 0.5` 추가, 4곳 + 7곳 교체

### 2. print → logging 전환
- `acc_gui.py`에 `logger = logging.getLogger(__name__)` 추가
- `print()` 7건 + `traceback.print_exc()` 7건 → `logger.error/warning/info/critical` + `exc_info=True`로 전환
- `main()`에 `logging.basicConfig()` 추가

### 3. bare except 수정
- `acc_gui.py` line ~4420: `except:` → `except (ValueError, AttributeError):`
- `pyproject.toml`: `E722` 규칙 ignore 목록에서 제거

### 4. plot_acc2() 리팩토링
- 356줄 단일 메서드를 8개 private 메서드 + ~35줄 오케스트레이터로 분리:
  - `_apply_acc1_style()`, `_draw_concentric_circles()`, `_draw_arc_lines()`
  - `_draw_radial_lines()`, `_draw_area_points()`, `_draw_merge_points()`
  - `_setup_plot_decorations()`, `_setup_interactivity()`
- `import copy`를 모듈 최상단으로 이동

### 5. GUI 테스트 추가
- `tests/gui/test_acc_gui.py`: 13건 신규 테스트
  - `compass_to_cart()` 4방향 테스트 (4건)
  - `get_resource_path()` 테스트 (1건)
  - `MainWindow` 인스턴스 생성/패널 확인 (2건)
  - `ACCVisualizationWidget` 초기화 (2건)
  - 코드 품질: print 없음, logger 존재, 리팩토링 메서드 존재, bare except 없음 (4건)
- `tests/conftest.py`에 `qapp` 세션 fixture 추가

### 6. 검증 결과
- `ruff check`: 수정 파일 0 warnings (기존 파일의 pre-existing issue는 유지)
- `ruff format`: 수정 파일 포맷 통과
- `pytest`: **84 tests passed** (기존 71 + 신규 13)

## 수정 파일 목록

| 파일 | 변경 유형 |
|------|----------|
| `acc_core.py` | 상수 추가 |
| `acc_core_new.py` | 상수 추가 |
| `acc_gui.py` | logging, bare except, 리팩토링, import 이동 |
| `pyproject.toml` | E722 제거 |
| `tests/conftest.py` | qapp fixture 추가 |
| `tests/gui/test_acc_gui.py` | 신규 |
| `CHANGELOG.md` | [Unreleased] 섹션 업데이트 |
